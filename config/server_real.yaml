# inference_server.py config — REAL robot environment.
# Mirrors this project's existing sim/real YAML-split convention (e.g.
# aruco_perception/config/aruco_detector_sim.yaml vs _real.yaml) instead of
# a separate script/port per environment.
#
# Select via: python3 inference_server.py --env real  (or INFERENCE_ENV=real)

model_path: "runs/detect/runs/aruco_cupholder-8/weights/best.pt"
host: "127.0.0.1"
port: 8600